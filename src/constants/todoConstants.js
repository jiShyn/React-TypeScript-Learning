const PREFIX = "todo";

export const GET_TODOS_PENDING = `${PREFIX}getTodos/pending`;
export const GET_TODOS_FULFILLED = `${PREFIX}getTodos/fulfilled`;
export const GET_TODOS_REJECTED = `${PREFIX}getTodos/rejected`;
export const CHANGE_FILTER = `${PREFIX}changeFilter`;
export const CHANGE_SEARCH = `${PREFIX}changeSearch`;
